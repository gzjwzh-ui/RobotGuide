# 展厅机器人助手 - 完整部署指南

## 🎯 系统架构

```
┌──────────────────────────┐    局域网     ┌─────────────────────────┐
│  电脑/服务器 (运行后端)   │◄──────────►│  Android 机器人         │
│  Python Flask API :5000   │  自动同步     │  人脸检测 + AI对话       │
│                           │              │  多语言语音             │
│  网页管理后台 admin.html  │              │  后端配置拉取           │
│  (浏览器直接打开)         │              │  本机问答库备份         │
└──────────────────────────┘              └─────────────────────────┘
```

## 📁 项目结构

```
RobotGuide/
├── backend/                     # 后端服务（电脑上跑）
│   ├── app.py                   # Flask API + 内嵌数据库
│   ├── admin.html               # 网页管理后台（打开 http://localhost:5000）
│   ├── requirements.txt         # Python依赖
│   ├── 启动后台.bat             # 一键启动（Windows双击）
│   └── robot.db                 # SQLite数据库（自动生成）
│
├── app/                         # Android APP
│   ├── build.gradle
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/robot/guide/
│       │   ├── App.kt
│       │   ├── api/             # 豆包AI客户端
│       │   ├── db/              # SQLite问答库
│       │   ├── data/            # 数据模型
│       │   ├── matcher/         # 智能问答匹配
│       │   ├── ui/              # 所有界面
│       │   └── util/            # 
│       │       ├── AppSettings.kt    # 设置管理
│       │       ├── BackendSync.kt    # 后端同步
│       │       ├── RobotTTS.kt       # 多语言TTS
│       │       └── PersonDetector.kt # 人脸检测自动唤醒
│       └── res/
│
├── README.md                    # 基础说明
└── DEPLOY.md                    # 你正在看的文件
```

## 🚀 第一步：启动后端（电脑上运行）

### 1. 安装 Python（如果还没装）
下载: https://www.python.org/downloads/
安装时勾选 ✅ "Add Python to PATH"

### 2. 启动后端
**双击运行** `backend\启动后台.bat`

首次运行会自动安装依赖，之后每次启动显示：
```
本机IP: 192.168.1.100

👉 网页管理后台: http://localhost:5000
👉 局域网设备访问: http://192.168.1.100:5000
👉 机器人APP后端地址填: http://192.168.1.100:5000
```

**重要**: 电脑和机器人必须在**同一个WiFi/局域网**！

### 3. 打开网页管理后台
在电脑浏览器访问: http://localhost:5000

你可以在网页上：
- ⚙️ **机器人配置**: 改名字、换语言（粤语/普通话/英语）、改开场白
- 📚 **问答库管理**: 添加/编辑/删除固定问答，支持JSON批量导入
- 🔗 **API设置**: 豆包API Key、模型ID、系统提示词

## 📱 第二步：编译 Android APP

### Android Studio 打开项目
1. File → Open → 选择 `RobotGuide` 文件夹
2. 等待 Gradle 同步（首次下载依赖，耐心等）
3. Build → Build Bundle(s) / APK(s) → Build APK(s)
4. 得到 `app-debug.apk`

### 安装到机器人
通过 USB / U盘 / 网盘 把 APK 拷到机器人，允许「未知来源」后安装。

### 机器人首次配置
打开 APP → 右下角齿轮 ⚙️ → 设置：

| 字段 | 填写 |
|------|------|
| 后端URL | `http://你电脑的IP:5000` （比如 `http://192.168.1.100:5000`）|
| 自动同步 | ✅ 打开 |
| 启用人体检测 | ✅ 打开 |
| 豆包API Key | `YOUR_ARK_API_KEY` |
| 模型ID | `doubao-seed-character-260628` |
| 机器人名字 / 语言 / 开场白 | 会自动从后端同步过来 |

点**保存**，APP 会自动从后端拉取配置和问答库。

## ✨ 第三步：功能效果

### 🤖 自动问候流程
```
有人进入视野 → 前摄摄像头检测到人脸 
→ 自动打开对话界面 
→ 播放开场白（TTS语音 + 显示文字）
→ 等待用户提问
```

### 🌐 多语言支持
| 语言 | 语音识别 | TTS播报 | 豆包AI |
|------|---------|--------|--------|
| 普通话 zh-CN | ✅ | ✅ | ✅ |
| 粤语 yue-HK | ✅ | ✅ | ✅ |
| 英语 en-US | ✅ | ✅ | ✅ |

**注意**: 切换语言后，豆包AI的 system prompt 会自动生效，AI会用对应语言回答。

### 📚 问答匹配优先级
```
用户提问 
  → 精确匹配（100分）
  → 关键词匹配（70+分）
  → 包含匹配
  → N-gram相似度
  
  以上都没匹配到 → 调用豆包AI
  AI也不可用 → 兜底回复
```

### 🔄 网页后台操作流程
1. 电脑浏览器打开 http://localhost:5000
2. 改机器人名字为「小助手」
3. 切换语言为粤语
4. 添加新问答：展厅有什么展品？→ 我们展示AI机器人！
5. 点保存
6. 机器人APP自动同步（或下次打开时同步）

## 🔧 常见问题

### Q: 机器人连不上后端？
1. 确认电脑和机器人在同一WiFi
2. 检查电脑防火墙有没有拦截 Python
3. 用机器人浏览器访问 `http://电脑IP:5000/api/health` 看能不能返回 JSON

### Q: 人脸检测不触发？
1. 首次打开会请求相机权限，点允许
2. 确认设置里开启了「启用人体检测」
3. 前摄摄像头要能看到人

### Q: TTS 没声音？
1. 确认机器人音量不是静音
2. Android 6.0.1 可能系统自带 TTS 引擎需额外安装（如谷歌TTS或系统中文语音包）

### Q: 豆包API报错？
- 检查 API Key 和 模型ID
- 确认火山引擎账号开通了方舟服务
- 看后端日志有没有错误

### Q: 只想用固定问答不用AI？
在网页后台设置里关掉「启用AI辅助」即可。

## 📝 后续扩展

这个架构可以继续扩展：
- 🖥️ 多台机器人统一管理（后端存设备ID）
- 📊 对话数据分析（后端存聊天记录）
- 🎯 人脸识别精准问候（识别熟客直接叫名字）
- 📡 远程升级APK（后端放APK下载地址）
- 🎬 视频通话（后端做信令服务器）

## 📄 License
自用项目，祝展厅顺利开业！🎉
